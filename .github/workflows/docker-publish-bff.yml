name: publish-bff
on:
  push:
    branches: [ "main" ]
    paths:
      - "services/bff/**"
      - "Dockerfile"
      - ".github/workflows/docker-publish-bff.yml"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Docker login
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USER }}" --password-stdin

      - name: Build image
        run: |
          docker build \
            -t ${{ secrets.DOCKERHUB_USER }}/pjbl-bff:${{ github.sha }} \
            -t ${{ secrets.DOCKERHUB_USER }}/pjbl-bff:latest \
            --build-arg SERVICE_PATH=services/bff \
            --build-arg START_CMD="node dist/index.js" \
            --build-arg HAS_BUILD=true \
            .

      - name: Push image
        run: |
          docker push ${{ secrets.DOCKERHUB_USER }}/pjbl-bff:${{ github.sha }}
          docker push ${{ secrets.DOCKERHUB_USER }}/pjbl-bff:latest
